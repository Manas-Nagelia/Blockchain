{
  "name": "blockchain",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "start": "node ./src/index.ts",
    "build": "tsc",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "colors": "1.4.0",
    "typescript": "^4.5.4"
  }
}
